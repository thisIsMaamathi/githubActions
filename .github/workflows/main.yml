name: Build and run test

on:
  push:
    branches:
      - testing

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v3

      - name: Build code
        run: npm ci

      - name: Runs tests
        run: npm run test
